import React from 'react';
import preloader from "../../../Logotip/preloader.svg"


const Preloader = () => {
    return (
        <div>
            <img src={preloader} alt="pic" className="Preloader" style={ {backgroundColor: '#ca93db'} }/> : null}
        </div>
    );
};

export default Preloader;